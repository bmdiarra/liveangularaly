import { PipeseparatorPipe } from './pipeseparator.pipe';

describe('PipeseparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeseparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
